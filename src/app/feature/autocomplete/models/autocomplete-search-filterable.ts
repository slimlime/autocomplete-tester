/**
 * Autocomplete search filterable
 */
export interface AutocompleteSearchFilterable {
  /**
   * Simple name
   *
   */
  name: string;

  /**
   * ViewModel details to display for autocomplete display function
   * @returns display name
   */
  // A getDisplayName(): string;
}
